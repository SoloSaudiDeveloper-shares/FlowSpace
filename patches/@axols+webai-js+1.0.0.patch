diff --git a/node_modules/@axols/webai-js/dist/index.mjs b/node_modules/@axols/webai-js/dist/index.mjs
index f4c0dfb..5a90c98 100644
--- a/node_modules/@axols/webai-js/dist/index.mjs
+++ b/node_modules/@axols/webai-js/dist/index.mjs
@@ -456,10 +456,8 @@ var ModelManager = class {
       );
       console.log(`Download started with ID: ${downloadId}`);
     } catch (error) {
-      throw createWorkerError(
-        `Failed to log download start: ${error instanceof Error ? error.message : "Unknown error"}. Download aborted.`,
-        "initialization_failed"
-      );
+      console.warn("[WebAI] Download telemetry failed (non-fatal):", error instanceof Error ? error.message : error);
+      downloadId = null;
     }
     return new Promise((resolve, reject) => {
       let lastProgressUpdate = 0;
